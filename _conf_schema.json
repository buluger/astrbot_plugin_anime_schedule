{
  "perm_mode_default": {
    "description": "默认操作权限：1=仅Bot管理员，2=仅群主，3=群主+管理员，4=全员",
    "type": "int",
    "hint": "作用于上传/删除/移动/交换/清空/设置/推送；群内可用「番剧权限」覆盖",
    "obvious_hint": true,
    "default": 3
  },
  "max_per_day_default": {
    "description": "每天默认可添加番剧数量上限",
    "type": "int",
    "hint": "1~20，群内可用「番剧设置 每日上限」覆盖",
    "obvious_hint": true,
    "default": 10
  },
  "push_hour_default": {
    "description": "默认定时推送小时（0~23）",
    "type": "int",
    "hint": "群内可用「番剧推送 时间」覆盖",
    "obvious_hint": true,
    "default": 8
  },
  "push_minute_default": {
    "description": "默认定时推送分钟（0~59）",
    "type": "int",
    "hint": "群内可用「番剧推送 时间」覆盖",
    "obvious_hint": true,
    "default": 0
  }
}
